1. добавить нужные библиотеки
pillow, wheel
django.contribe.sites , SITE_ID=1
переименовать базу данных

2. Настройки settings.py

добавить статику
STATIC_ROOT= os.path.join(BASE_DIR, 'static')
MEDIA_URL='/media/'


LANGUAGE_CODE = 'ru-ru'

TIME_ZONE = 'Europe/Moscow'

STATICFILES_FINDERS=[
    'django.contrib.staticfiles.finders.FileSystemFinder',
    'django.contrib.staticfiles.finders.AppDirectoriesFinder',
]


STATICFILES_DIRS = [
]

ADMINS=[('Andrew', 'andrewerm@yandex.ru'),]

ALLOWED_HOSTS = ['*']

3. определить архитектуру проекта
Python
Django
HTML
XML
JSON
CSS / Bootstrap
JQuery
The DOM
Ajax
JavaScripts


1.собираем в initializr то что нужно
2. берем оттуда стр. 404, кладём в templates и так же копируем её как стр. 500
3. кладём в проект в папку .\collectstatic
STATICFILES_DIRS = [
os.path.join(BASE_DIR, 'collectstatic')
]
4. потом командой collectstatic собираем


{% load staticfiles %} прописать в базовом шаблоне
поменять на <link rel="stylesheet" href="{% static "initializr/css/bootstrap.min.css" %}>

создаем
1. приложение: startapp jsapp
2. templates и stati
3. urls
4. в views  - from django.template.response import TemplateResponse
5. в папке приложения создаем в templates папку по имени приложения и туда html
5. в views функцию вывода страницы return  TemplateResponse(request, 'jsapp/jsapp_index.html')